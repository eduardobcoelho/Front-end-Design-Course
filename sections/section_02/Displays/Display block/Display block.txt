1 - the display block accepts width and height.
2 - A display block occupies all the line, so, isn't possible to have elements side by side in a display block.
3 - When we don't set the width in a display block element, the elemento occupies all the screen's width.